import 'package:flutter/material.dart';

class TagProductsScreen extends StatelessWidget {
  const TagProductsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
