import 'package:flutter/material.dart';

class TagPostsScreen extends StatelessWidget {
  const TagPostsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
