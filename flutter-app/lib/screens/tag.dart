import 'package:flutter/material.dart';

class TagScreen extends StatelessWidget {
  final String tag;
  const TagScreen({required this.tag, Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
