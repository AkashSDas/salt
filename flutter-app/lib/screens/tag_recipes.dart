import 'package:flutter/material.dart';

class TagRecipesScreen extends StatelessWidget {
  const TagRecipesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
